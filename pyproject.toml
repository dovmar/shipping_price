[project]
name = "shipping_price"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.8,<4.0"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=6.0",
    "pytest-cov>=4.0",
    "ruff>=0.0.282",
]

[tool.ruff]
line-length = 88
